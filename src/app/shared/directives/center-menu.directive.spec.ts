import { CenterMenuDirective } from './center-menu.directive';

describe('CenterMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new CenterMenuDirective();
    expect(directive).toBeTruthy();
  });
});
